<h1>All alpha Blogers</h1>
<% @users.each do |user| %>
    <ul>
        <div>
            <li><%= link_to 'user avatar', user_path(user)%></li>
            <li><%= link_to user.username, user_path(user)%></li>
            <li><%= pluralize(user.articles.count, "article") if user.articles %></li>
        </div>
    </ul>

<% end %>